DATABASE_URL="file:./prisma/dev.db"

# Site access (see src/lib/auth.ts / src/proxy.ts). User accounts live in the
# database (User model); create one with: npx tsx scripts/create-user.ts <username> <password>
AUTH_SECRET="generate-a-long-random-string"
